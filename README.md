# 222222222222222222222222
anime guess game

run python file first: it generates charcters.json based on image files provided.
then run python -m http.server --bind 127.0.0.1 8000 to start the local host .
then in browser type localhost http://localhost:8000/ to start the game.
